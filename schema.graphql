type Todo {
    id: String
    title: String
}

type Query {
    getAllTodos: [Todo]
}

type Mutation {
    createTodo(title: String): Todo
    removeTodo(id: String): Todo
    editTodo(id: String, title: String): Todo
}